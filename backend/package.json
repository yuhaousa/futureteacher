{
  "name": "futureteacher-api",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute futureteacher-db --local --file=migrations/0001_schema.sql && wrangler d1 execute futureteacher-db --local --file=migrations/0002_seed.sql",
    "db:migrate:remote": "wrangler d1 execute futureteacher-db --file=migrations/0001_schema.sql && wrangler d1 execute futureteacher-db --file=migrations/0002_seed.sql"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "wrangler": "^3.91.0"
  }
}
